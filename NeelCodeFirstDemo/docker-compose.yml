version: "3"
services:
  web:
      build: dotnet
      ports:
        - "8000:80"
         - "443;443"
      links:
        - db      
  db:
    image: mysql:5.7
    volumes:
     - /var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=Rocklin123
     - MYSQL_DATABASE=database